# Forge BPMN — Marketplace listing (UX draft)

**Name:** Forge BPMN  
**Vendor:** Forge · kenneth96@outlook.com  
**Id:** `com.forge.bpmn` · v0.4.2

## Short description (plugin.xml / Marketplace blurb)
Edit BPMN 2.0 diagrams inside IntelliJ IDEA — canvas, properties, XML round-trip, and Token Simulation with New UI–aligned chrome.

## Full description (HTML for plugin.xml)
<p><b>Forge BPMN</b> opens <code>.bpmn</code> files in a Camunda Modeler–style editor inside IntelliJ IDEA.</p>
<ul>
  <li>Diagram canvas + properties panel (bpmn-js Cloud)</li>
  <li>BPMN ↔ XML tabs with round-trip sync</li>
  <li>Token Simulation with quiet New UI gray chips</li>
  <li>Light and Dark theme sync with the IDE</li>
  <li>BPMN Explorer tool window</li>
</ul>

## Screenshot set (upload order)
1. Editor + Token Sim (dark) — show gray chips, no loud green pads
2. Gateway pads side-by-side (Trigger + Set Sequence Flow)
3. Light theme canvas (if available)
4. Empty explorer / empty properties (polish)

## Icon
- `pluginIcon.svg` — gateway diamond + accent play `#3574F0` on `#2B2D30`
- Replace `src/main/resources/META-INF/pluginIcon.svg` when Kenneth approves
